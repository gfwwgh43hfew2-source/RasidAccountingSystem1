<Window x:Class="RasidAccountingSystem.Views.CollectInstallmentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RasidAccountingSystem.Views"
        Title="تحصيل قسط" 
        Height="450" 
        Width="550"
        MinHeight="400"
        MinWidth="500"
        WindowStartupLocation="CenterOwner"
        Background="#F8FAFC"
        FontFamily="Segoe UI"
        FlowDirection="RightToLeft"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#6366F1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="MinWidth" Value="100"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4F46E5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#E2E8F0"/>
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="MinWidth" Value="100"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#CBD5E1"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- العنوان -->
        <Border Grid.Row="0" Background="#6366F1" CornerRadius="10" Padding="20,15" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="💰 تحصيل قسط" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           Foreground="White"
                           HorizontalAlignment="Center"/>
                <TextBlock x:Name="txtInvoiceInfo" 
                           Text="العميل: أحمد محمد | الفاتورة: SIN-000001 | القسط: 1" 
                           FontSize="14" 
                           Foreground="#E0E7FF"
                           HorizontalAlignment="Center"
                           Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <!-- محتوى النافذة -->
        <StackPanel Grid.Row="1">
            <Border Background="White" CornerRadius="10" Padding="18" Margin="0,0,0,18" BorderBrush="#E2E8F0" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="المبلغ المتبقي:" FontSize="14" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,15,10"/>
                    <TextBlock x:Name="lblRemainingAmount" Grid.Row="0" Grid.Column="1" Text="0.00 ر.س" FontSize="16" FontWeight="Bold" Foreground="#D97706" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="المبلغ المحصل:" FontSize="14" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,15,10"/>
                    <TextBox x:Name="txtAmount" Grid.Row="1" Grid.Column="1" Text="0.00" FontSize="16" FontWeight="SemiBold" Height="38" Padding="10,0" Background="#F8FAFC" BorderBrush="#CBD5E1" BorderThickness="1" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="طريقة الدفع:" FontSize="14" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,15,10"/>
                    <ComboBox x:Name="cmbPaymentMethod" Grid.Row="2" Grid.Column="1" Height="38" FontSize="14" Margin="0,0,0,10" SelectedIndex="0">
                        <ComboBoxItem Content="نقدي"/>
                        <ComboBoxItem Content="تحويل بنكي"/>
                        <ComboBoxItem Content="شيك"/>
                    </ComboBox>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="الخزينة:" FontSize="14" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,15,10"/>
                    <local:SmartSearchComboBox x:Name="cmbTreasury" Grid.Row="3" Grid.Column="1" Height="38" FontSize="14" Margin="0,0,0,10" DisplayMemberPath="Name" SelectedValuePath="Id" Placeholder="اكتب اسم الخزينة..."/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="ملاحظات:" FontSize="14" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,15,0"/>
                    <TextBox x:Name="txtNotes" Grid.Row="4" Grid.Column="1" Height="38" FontSize="14" Padding="10,0" Background="#F8FAFC" BorderBrush="#CBD5E1" BorderThickness="1"/>
                </Grid>
            </Border>
        </StackPanel>

        <!-- الأزرار -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button x:Name="btnConfirm" Style="{StaticResource ModernButton}" Content="✅ تأكيد" Width="140" Height="45" FontSize="15" Click="BtnConfirm_Click"/>
            <Button x:Name="btnCancel" Style="{StaticResource SecondaryButton}" Content="❌ إلغاء" Width="140" Height="45" FontSize="15" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>