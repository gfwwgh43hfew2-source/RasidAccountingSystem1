<UserControl x:Class="RasidAccountingSystem.Views.ChecksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="#F8FAFC"
             FlowDirection="RightToLeft"
             FontFamily="Segoe UI">

    <UserControl.Resources>

        <SolidColorBrush x:Key="PrimaryColor" Color="#4361EE"/>
        <SolidColorBrush x:Key="PrimaryDark" Color="#3A56D4"/>
        <SolidColorBrush x:Key="SuccessColor" Color="#10B981"/>
        <SolidColorBrush x:Key="DangerColor" Color="#EF4444"/>
        <SolidColorBrush x:Key="WarningColor" Color="#F59E0B"/>
        <SolidColorBrush x:Key="SurfaceColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextPrimaryColor" Color="#1E293B"/>
        <SolidColorBrush x:Key="TextSecondaryColor" Color="#64748B"/>
        <SolidColorBrush x:Key="BorderLight" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="Gray50" Color="#F8FAFC"/>

        <DropShadowEffect x:Key="ShadowSm" BlurRadius="6" ShadowDepth="1" Opacity="0.08" Color="#000000"/>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryDark}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SurfaceColor}"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryColor}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{StaticResource BorderLight}" BorderThickness="1" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource Gray50}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderLight}"/>
        </Style>

        <Style x:Key="StatusBadge" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="10,4"/>
        </Style>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- الشريط العلوي -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceColor}" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" Effect="{StaticResource ShadowSm}">
            <Grid Margin="15,10,15,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- بطاقات الملخص -->
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="#ECFDF5" Padding="14" Margin="0,0,6,0" CornerRadius="10">
                        <StackPanel>
                            <TextBlock Text="📥 شيكات واردة معلَّقة" FontSize="11" Foreground="#065F46"/>
                            <TextBlock x:Name="lblReceivablePending" Text="0.00" FontSize="19" FontWeight="Bold" Foreground="{StaticResource SuccessColor}"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Background="#FEF2F2" Padding="14" Margin="6,0" CornerRadius="10">
                        <StackPanel>
                            <TextBlock Text="📤 شيكات صادرة معلَّقة" FontSize="11" Foreground="#991B1B"/>
                            <TextBlock x:Name="lblPayablePending" Text="0.00" FontSize="19" FontWeight="Bold" Foreground="{StaticResource DangerColor}"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Background="#FFFBEB" Padding="14" Margin="6,0" CornerRadius="10">
                        <StackPanel>
                            <TextBlock Text="⏰ مستحقة خلال 7 أيام" FontSize="11" Foreground="#92400E"/>
                            <TextBlock x:Name="lblDueSoon" Text="0" FontSize="19" FontWeight="Bold" Foreground="{StaticResource WarningColor}"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="3" Background="#FEF2F2" Padding="14" Margin="6,0,0,0" CornerRadius="10">
                        <StackPanel>
                            <TextBlock Text="↩️ شيكات مرتدة" FontSize="11" Foreground="#991B1B"/>
                            <TextBlock x:Name="lblBounced" Text="0" FontSize="19" FontWeight="Bold" Foreground="{StaticResource DangerColor}"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- أدوات التحكم -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="btnAddCheck" Grid.Column="0" Content="➕ شيك جديد" Width="120" Height="36" Style="{StaticResource PrimaryButton}" Margin="0,0,10,0"/>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,10,0">
                        <ComboBox x:Name="cmbTypeFilter" Width="140" Height="34" Style="{StaticResource ModernComboBoxStyle}" Margin="0,0,8,0"/>
                        <ComboBox x:Name="cmbStatusFilter" Width="150" Height="34" Style="{StaticResource ModernComboBoxStyle}"/>
                    </StackPanel>

                    <Button x:Name="btnRefresh" Grid.Column="3" Content="⟳ تحديث" Width="80" Height="36" Style="{StaticResource SecondaryButton}"/>
                </Grid>
            </Grid>
        </Border>

        <!-- قائمة الشيكات -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceColor}" Margin="20" BorderBrush="{StaticResource BorderLight}" BorderThickness="1" CornerRadius="12" Effect="{StaticResource ShadowSm}" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="lblNoChecks" Grid.Row="1" Text="لا توجد شيكات مطابقة لهذا الفلتر" FontSize="13" Foreground="{StaticResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>

                <DataGrid x:Name="dgChecks" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="None" RowHeight="46" BorderThickness="0" Background="{StaticResource SurfaceColor}" AlternatingRowBackground="{StaticResource Gray50}" RowBackground="{StaticResource SurfaceColor}">
                    <DataGrid.Columns>

                        <DataGridTextColumn Header="النوع" Binding="{Binding TypeDisplay}" Width="90">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Center"/><Setter Property="FontSize" Value="12"/></Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="رقم الشيك" Binding="{Binding CheckNumber}" Width="110">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Center"/><Setter Property="FontSize" Value="12"/><Setter Property="FontWeight" Value="SemiBold"/></Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="العميل / المورد" Binding="{Binding PartyName}" Width="*" MinWidth="140">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Right"/><Setter Property="FontSize" Value="12"/></Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="البنك" Binding="{Binding BankName}" Width="130">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Right"/><Setter Property="FontSize" Value="12"/></Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="تاريخ الاستحقاق" Binding="{Binding DueDateDisplay}" Width="110">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Center"/><Setter Property="FontSize" Value="12"/></Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="القيمة" Binding="{Binding FormattedAmount}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Center"/><Setter Property="FontSize" Value="12"/><Setter Property="FontWeight" Value="SemiBold"/></Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTemplateColumn Header="الحالة" Width="140">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource StatusBadge}" Background="{Binding StatusBackground}" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding StatusDisplay}" FontSize="11" FontWeight="SemiBold" Foreground="{Binding StatusForeground}"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="" Width="220">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="تغيير الحالة" Width="95" Height="30" FontSize="10" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Click="ChangeStatus_Click" Tag="{Binding CheckId}"/>
                                        <Button Content="تعديل" Width="55" Height="30" FontSize="10" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Click="EditCheck_Click" Tag="{Binding CheckId}"/>
                                        <Button Content="إلغاء" Width="55" Height="30" FontSize="10" Style="{StaticResource SecondaryButton}" Click="VoidCheck_Click" Tag="{Binding CheckId}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
