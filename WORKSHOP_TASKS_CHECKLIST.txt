===================================================================
خطة عمل تحويل "الورشة" — قائمة مهام صغيرة قابلة للتنفيذ جلسة بجلسة
===================================================================
هذا الملف موجود عشان لو انتهى حد الاستخدام (Credit) في نص الشغل،
أي جلسة/حساب جديد يفتح المشروع يقدر يكمل بالظبط من حيث ما وقفنا،
من غير ما يحتاج يقرأ كل تاريخ المحادثة القديم.

-------------------------------------------------------------------
📌 تعليمات إلزامية لأي جلسة (بما فيها الجلسة الحالية) تشتغل على الملف ده
-------------------------------------------------------------------
1. اقرأ قسم "آخر توقف" تحت مباشرة الأول، قبل أي حاجة تانية.
2. نفّذ **مهمة واحدة بس** من "المهام القادمة" (أول عنصر لسه مش
   معلَّم [x]) — حتى لو المستخدم مديك رصيد/وقت أكتر، خليك عند مهمة
   واحدة إلا لو طلب صراحة غير كده.
3. بعد التنفيذ مباشرة:
   - علّم المهمة [x] في القايمة تحت.
   - اكتب سطرين تحت "سجل التنفيذ" (اسم المهمة + وصف مختصر لأي ملفات
     اتعدلت + أي نتيجة تحقق عملتها).
   - حدّث قسم "آخر توقف" بالمهمة الجديدة اللي خلصت.
4. سلّم ملف المشروع **.zip كامل** دايمًا — حتى لو التعديل سطر واحد.
   الملف الناقص أو "الفرق فقط" مالوش فايدة للمستخدم.
5. لو لقيت المهمة في القايمة أكبر من اللازم لجلسة واحدة، قسّمها لمهمتين
   أصغر في القايمة نفسها بدل ما تنفّذها كلها دفعة واحدة، وسيب تعليق
   يوضح إنك قسّمتها وليه.

-------------------------------------------------------------------
آخر توقف (حدِّث السطرين دول بعد كل مهمة)
-------------------------------------------------------------------
آخر مهمة اتنفذت بالكامل: #6 — بناء شاشة "استلام سيارة" (فورم إدخال
بيانات Placeholder، بتصميم مختلف عن نمط الكروت المتكررة في الشاشات
اللي قبلها — راجع سجل التنفيذ تحت للتفاصيل)
حالة المشروع: XML سليم لكل الملفات المعدَّلة + توازن أقواس سليم في
WorkshopCarIntakeView.xaml.cs وWorkshopMainWindow.xaml.cs + كل مفاتيح
StaticResource المستخدمة اتأكد وجودها في WorkshopTheme.xaml (تحقق آلي،
مش تشغيل فعلي في Visual Studio). المهمة الجاية: #7 — بناء شاشة "الفنيون".

-------------------------------------------------------------------
المهام القادمة (بالترتيب — واحدة بس في كل جلسة)
-------------------------------------------------------------------
[x] 1. ربط تنقّل أساسي: كل زرار في WorkshopSidebar يطلق SectionSelected
       عند الضغط، وWorkshopMainWindow يبدّل بين الـDashboard ورسالة
       عامة "لسه مش جاهزة" لأي قسم لسه مالوش شاشة حقيقية.
       (بدون تغيير شكل الزرار المضغوط نفسه بعد — ده المهمة الجاية)

[x] 2. تفعيل الشكل المرئي (Active State) للزرار المضغوط فعليًا في
       الـSidebar وقت التشغيل (مش بس "الرئيسية" ثابتة زي دلوقتي) —
       تعديل صغير في WorkshopSidebar.xaml.cs يبدّل Style الزرار
       القديم/الجديد عند كل ضغطة.

[x] 3. بناء شاشة "العملاء" الحقيقية الأولى (قائمة بيانات Placeholder
       شكلية — بدون قاعدة بيانات بعد) بدل رسالة "لسه مش جاهزة"، بنفس
       اللغة البصرية المستخدمة في الـDashboard (كروت/جدول بسيط).

[x] 4. بناء شاشة "السيارات" الحقيقية (نفس أسلوب المهمة 3).

[x] 5. بناء شاشة "أوامر الشغل" الحقيقية (قائمة + شارات الحالة الملوّنة
       الموجودة بالفعل في معاينة التصميم الأولى).

[x] 6. بناء شاشة "استلام سيارة" (فورم إدخال بيانات بسيط Placeholder).

[ ] 7. بناء شاشة "الفنيون".

[ ] 8. بناء شاشتَي "المخزن" و"الموردون".

[ ] 9. بناء شاشات "الفواتير"، "الحسابات"، "المصروفات".

[ ] 10. بناء شاشتَي "التقارير" و"الإعدادات".

[ ] 11. جولة تحقق بصري فعلي (المستخدم يشغّل ويبلّغ): مراجعة كل أيقونات
        Segoe Fluent Icons المستخدمة في كل الشاشات، واستبدال أي كود
        ظهر غلط/فاضي بكود صحيح.

[ ] 12. قرار معماري (يحتاج موافقة المستخدم أولًا، مش تنفيذ تلقائي):
        استبدال MainWindow.xaml الإنتاجي بهذا الهيكل الجديد بالكامل،
        أو الإبقاء عليه منفصل لحد اكتمال كل الأقسام. (راجع الخيارين
        أ/ب المشروحين بالتفصيل في PROJECT_PROGRESS.txt قبل التنفيذ)

[ ] 13. ربط الأرقام الحقيقية من قاعدة البيانات (DatabaseService) في
        الـDashboard بدل أرقام الـPlaceholder الشكلية الحالية.

-------------------------------------------------------------------
سجل التنفيذ (سطرين لكل مهمة اتعملت، بالترتيب الزمني)
-------------------------------------------------------------------
- المهمة 1 (تم): أضفت event باسم SectionSelected في
  WorkshopSidebar.xaml.cs بيتطلق من Click على أي زرار تنقّل (13 زرار،
  عبر Click="SidebarButton_Click" في XAML). في WorkshopMainWindow
  عملت ScrollViewer باسم DashboardView + Border جديد باسم NotReadyView
  (Collapsed افتراضيًا)، والكود بيبدّل بينهم حسب اسم القسم المضغوط في
  OnSidebarSectionSelected. تحقق: XML سليم للملفين + كل الـStaticResource
  (19 في MainWindow، 12 في Sidebar) متأكد وجودها في WorkshopTheme.xaml،
  وتوازن الأقواس في ملفي الـ.cs سليم.

- المهمة 2 (تم): أضفت x:Name="HomeButton" لزرار "الرئيسية" في
  WorkshopSidebar.xaml عشان يبقى نقطة انطلاق معروفة. في
  WorkshopSidebar.xaml.cs ضفت حقل _activeButton بياخد قيمته الابتدائية
  HomeButton في الكونستركتور، ودالة SetActiveButton بترجّع الزرار
  القديم لـSidebarItemStyle وتدّي الزرار الجديد المضغوط
  SidebarItemActiveStyle، بتتنادى من SidebarButton_Click قبل ما يتطلق
  SectionSelected. تحقق: XML سليم + توازن الأقواس سليم في الـ.cs.

- المهمة 3 (تم): أنشأت Views/WorkshopCustomersView.xaml (+.xaml.cs) —
  UserControl جديد لشاشة "العملاء" الحقيقية الأولى، بنفس اللغة البصرية
  المستخدمة في DashboardView (StatTileStyle لثلاث بلاطات: إجمالي
  العملاء/عليهم مستحقات/عملاء جدد، وجدول Border+Grid يدوي بأربعة صفوف
  Placeholder ثابتة باسم/هاتف/لوحة/رصيد/حالة، مع StatusBadgeBaseStyle
  الموجود أصلًا للحالة). في WorkshopMainWindow.xaml أضفت
  <views:WorkshopCustomersView x:Name="CustomersView"/> بجوار
  DashboardView وNotReadyView (Collapsed افتراضيًا). في
  WorkshopMainWindow.xaml.cs عدّلت OnSidebarSectionSelected: بقى بيخفي
  الثلاث شاشات الأساسية الأول، بعدين يظهر واحدة بس حسب اسم القسم —
  "العملاء" بقى ليها فرع مستقل بيظهر CustomersView بدل ما يدخل في مسار
  NotReadyView العام. بدون DB بعد — كل الأرقام والصفوف ثابتة في XAML.
  تحقق: XML سليم للملفين الجديدين + توازن أقواس سليم في الـ.cs، وكل
  الـStaticResource المستخدمة (TextH2Style، StatTileStyle،
  StatusBadgeBaseStyle، إلخ) متأكد وجودها في WorkshopTheme.xaml.

- المهمة 4 (تم): أنشأت Views/WorkshopCarsView.xaml (+.xaml.cs) — بنفس
  هيكل WorkshopCustomersView.xaml بالظبط: 3 بلاطات (إجمالي السيارات
  المسجلة/داخل الورشة الآن/جاهزة للتسليم) وجدول Border+Grid يدوي بأربعة
  صفوف Placeholder (لوحة/موديل/مالك/تاريخ دخول/حالة)، مستخدمة ألوان
  StatusInspection/StatusInProgress/StatusWaitingParts/StatusReady
  الموجودة أصلًا في WorkshopTheme.xaml لشارات الحالة. في
  WorkshopMainWindow.xaml أضفت <views:WorkshopCarsView x:Name="CarsView"/>
  بجوار CustomersView، وفي WorkshopMainWindow.xaml.cs ضفت فرع "السيارات"
  في OnSidebarSectionSelected بنفس نمط "العملاء". **مهم:** اكتشفت إن
  المشروع .csproj قديم الطراز (بيسرد كل ملف صراحة، مفيش Wildcard) —
  فضفت Compile+Page entries للملف الجديد يدويًا في هذه الخطوة (ونفس
  الحاجة كانت لازمة للمهمة 3 واتعملت فيها بعد الاكتشاف). تحقق: XML سليم
  للملفين الجديدين + توازن أقواس سليم + كل الـStaticResource المستخدمة
  متأكد وجودها في WorkshopTheme.xaml + csproj سليم XML-wise وفيه entry
  واحد بالظبط لكل ملف جديد (لا تكرار).

- المهمة 5 (تم): أنشأت Views/WorkshopWorkOrdersView.xaml (+.xaml.cs) —
  4 بلاطات إحصائية (أوامر مفتوحة/منتظرة قطع غيار/جاهزة للتسليم/مكتملة
  اليوم) وجدول Border+Grid يدوي بـ7 صفوف (رقم أمر/سيارة/عميل/فني
  مسؤول/تاريخ فتح/حالة)، كل صف بحالة مختلفة تغطي كل شارات الحالة السبعة
  الموجودة في WorkshopTheme.xaml (جديد/قيد الفحص/قيد التنفيذ/منتظر قطع
  غيار/جاهز للتسليم/تم التسليم/ملغي) — أول شاشة في المشروع تستخدمهم
  كلهم مع بعض. في WorkshopMainWindow.xaml أضفت
  <views:WorkshopWorkOrdersView x:Name="WorkOrdersView"/> بجوار
  CarsView، وفي WorkshopMainWindow.xaml.cs ضفت فرع "أوامر الشغل" في
  OnSidebarSectionSelected بنفس النمط. ضفت Compile+Page entries
  للملفين الجديدين في .csproj (نفس الخطوة المطلوبة من كل شاشة جديدة).
  تحقق: XML سليم للملفين الجديدين + توازن أقواس سليم + كل الـ7
  StatusBrush متأكد وجودها في WorkshopTheme.xaml + csproj سليم وفيه
  entry واحد بالظبط لكل ملف جديد.

- المهمة 6 (تم): بنيت Views/WorkshopCarIntakeView.xaml (+.xaml.cs) —
  فورم "إيصال استلام سيارة" بتصميم مبني على شكل إيصال ورقي حقيقي بدل
  تكرار قالب "3 بلاطات إحصائية بشريط لون" المستخدم في شاشتي العملاء
  والسيارات (رد مباشر على ملاحظة إن التصميم شكله متكرر/قوالب جاهزة):
  هيدر داكن برقم إيصال وتاريخ، خط تقطيع (Tear Line) بخط متقطّع حقيقي،
  رقم لوحة بشكل شبيه بلوحة سيارة فعلية (خلفية داكنة + حدّ برتقالي)،
  عداد كيلومترات بخط Consolas على خلفية داكنة زي عداد حقيقي، مقياس
  بنزين كأجزاء بصرية (5 قطع) بدل progress bar جاهز، فحص حالة خارجية
  بـ4 صفوف (أمام/خلف/يمين/شمال) كل صف فيه 3 اختيارات RadioButton ملوّنة
  (سليم=أخضر/خدش=كهرماني/تلف=أحمر) بدل شارات Pill عادية، وكعب الإيصال
  في الآخر فيه خانتا توقيع (عميل/مستقبل) زي أي إيصال ورقي فعلي. الظل
  (ElevatedShadowEffect) اتحط مرة واحدة بس على كارت الإيصال ككل، مش على
  كل عنصر جواه، بنفس مبدأ WorkshopTheme.xaml. وصّلت الشاشة بـ
  WorkshopMainWindow.xaml (إضافة WorkshopCarIntakeView x:Name="CarIntakeView")
  وWorkshopMainWindow.xaml.cs (إخفاؤها مع باقي الشاشات + فرع "استلام
  سيارة" في OnSidebarSectionSelected)، وضفت Compile+Page entries
  للملفين الجديدين في RasidAccountingSystem.csproj. تحقق: XML سليم لكل
  ملف معدَّل + توازن أقواس سليم في WorkshopCarIntakeView.xaml.cs
  وWorkshopMainWindow.xaml.cs + كل StaticResource المستخدمة (منها
  محلية زي FieldBoxStyle/ConditionOkStyle ومنها من WorkshopTheme.xaml)
  اتأكد وجودها + csproj سليم XML-wise بـentry واحد بالظبط لكل ملف جديد.

  ملاحظة تصميم مفتوحة (مش منفَّذة، تحتاج قرارك): شاشتي "العملاء"
  و"السيارات" (المهمتين 3 و4) لسه بيستخدموا قالب "3 بلاطات إحصائية
  متطابقة بشريط لون" المتكرر، وهو نفس النمط اللي WorkshopTheme.xaml
  نفسه بيحذّر منه في تعليق قسم الـDashboard. لو حبيت، ممكن نعمل مهمة
  منفصلة لاحقًا لتوحيدهم على تسلسل HeroMetricCardStyle/LedgerStripStyle
  المعرَّف بالفعل في الملف بدل تكرار StatTileStyle 3 مرات.
