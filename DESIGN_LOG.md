# سجل إعادة التصميم - نظام رصيد المحاسبي (RasidAccountingSystem)

هذا الملف مستقل عن PROGRESS_LOG.txt (بتاع تصحيح الأكواد)، وبيوثّق خطوات
إعادة التصميم البصري الجديدة بالكامل بعد ما اتفقد الزيب القديم اللي كان
فيه هوية "ledger & seal" (بورجندي/نحاسي).

الهوية الجديدة المعتمدة: **"الخزينة الحديثة" (Modern Vault)**
- أساسي: أخضر زمردي غامق `#0F5C46`
- تمييز: ذهبي/عنبري `#C9972C`
- خلفية: عاجي دافئ `#FAF7F0`
- كل التدرجات الرمادية والنصية تحوّلت لعائلة "دافئة" (Warm Taupe) بدل
  الرمادي البارد (Slate) القديم.

مطلوب: تطبيق الهوية على **البرنامج بالكامل**، مش شاشات منفردة.

---------------------------------------------------------------
Part 1 — نظام الألوان المركزي (App.xaml)
---------------------------------------------------------------
الملف: App.xaml (Application.Resources)

كل الأنماط والقوالب في المشروع (الأزرار، الحقول، الجداول، القوائم
المنسدلة، الكروت، أشرطة التمرير...) كانت بالفعل بتستخدم موارد ملوّنة
مركزية بأسماء ثابتة (PrimaryColor, BackgroundColor, TextPrimaryColor,
Gray50...Gray900, SuccessColor, WarningColor...) بدل الألوان الصريحة
داخل كل Style. هذا يعني إن تغيير قيم الموارد دي بس، كفيل إنه يغيّر شكل
البرنامج بالكامل تلقائيًا بدون لمس أي View.

التعديلات:
1. استبدال كل قيم الألوان الأساسية (Primary/Background/Text/Success/
   Warning/Danger/Info/Border/Gray50-900/MessageBox*/SidebarGradient)
   بالهوية الجديدة (أخضر زمردي + عاجي دافئ + رمادي دافئ) — تم بحرص شديد
   عن طريق فحص كل قيمة hex في الملف وتحديد أماكن استخدامها الفعلية
   (بعض القيم كانت مُستخدمة بالصدفة لأكتر من غرض بنفس الرقم، فاتعمل لها
   استبدال منفصل بدل استبدال شامل عشان محدش ياخد قيمة غلط).
2. تحديث كل الألوان الصريحة (hardcoded) المرتبطة بحالات hover/pressed/
   disabled في الأزرار (Primary/Success/Warning/Secondary buttons)
   وأشرطة التمرير (ThinScrollBar/SimpleScrollBar) والقائمة الجانبية
   (SidebarMainButton/SidebarSubButton) لتتماشى مع الهوية الجديدة.
3. إضافة عائلة ألوان جديدة لم تكن موجودة من قبل: AccentGoldColor/
   AccentGoldDarkColor/AccentGoldLightColor/AccentGoldSoftColor —
   جاهزة للاستخدام في الخطوات الجاية (شارات، تمييزات، عناصر VIP).
4. تم التحقق من توازن الوسوم بعد التعديل (597 فتح / 597 قفل لعلامات
   `<` `>`، و100/100 للأقواس المعقوفة).

النتيجة: كل شاشة في البرنامج (اللي بتستخدم الموارد المركزية دي، وهو
الغالبية العظمى) هتتلون تلقائيًا بالهوية الجديدة فور تشغيل المشروع،
من غير أي تعديل إضافي.

---------------------------------------------------------------
Part 2 — MainWindow.xaml (الإطار الخارجي + الشريط الجانبي + الهيدر)
---------------------------------------------------------------
الملف: Views/MainWindow.xaml

اتضح إن الملف ده معندوش موارد ألوان مركزية من App.xaml بس — عنده كمان
مجموعة موارد محلية خاصة بيه (SidebarBg, SidebarHover, SidebarActive,
BorderColor, TextLight, TextWhite, HeaderText, Notification*...)
بالإضافة لعدد كبير من الألوان المكتوبة صراحة (hardcoded) جوه الملف
نفسه (فواصل القائمة الجانبية، بوردرات الهيدر، نافذة التنبيهات...).

التعديلات:
1. تحويل الشريط الجانبي بالكامل من "كحلي غامق" (#0F172A/#1E293B) إلى
   "أخضر زمردي غامق جدًا" (#0B2E22/#123D2E) يتماشى مع الهوية الجديدة.
2. **أول استخدام فعلي حقيقي للون الذهبي (AccentGold)**: مورد
   SidebarActive (لتمييز العنصر النشط في القائمة الجانبية) بقى
   `#C9972C` بدل الأزرق القديم، مع خلفية داكنة ذهبية مصاحبة له
   (SidebarActiveBg بقت #3D2F14) — جاهزين للاستخدام فور ما تتفعّل
   منطق تمييز العنصر النشط في القائمة (كانا موجودين كموارد بس مش
   مستخدمين فعليًا في الكود قبل كده).
3. كل الفواصل بين مجموعات القائمة الجانبية (11 فاصل) وحدودها اتلونت
   بلون أخضر داكن متناسق (#1F4436) بدل الرمادي الكحلي القديم.
4. نصوص القائمة والهيدر (TextLight/TextWhite/HeaderText) اتحدثت
   لتدرجات دافئة (عاجي فاتح على الأخضر الغامق، بني داكن دافئ على
   الأبيض) بدل الأبيض/الرمادي البارد.
5. نافذة التنبيهات المنسدلة (Popup) وشريط الهيدر العلوي والفوتر:
   كل الحدود والخلفيات الفاتحة (#E2E8F0, #F8FAFC) والنصوص الرمادية
   (#94A3B8, #64748B, #1E293B) اتحولت للعائلة الدافئة الموحّدة نفسها
   المستخدمة في App.xaml (BorderLight/BackgroundColor/TextMuted/
   TextPrimary الجديدة)، وألوان النجاح/التحذير/الخطأ في التنبيهات
   اتوحدت مع SuccessColor/WarningColor/DangerColor الجديدة.
6. نصوص "مرحباً/اسم المستخدم" وروابط التنبيهات (تحديد الكل كمقروء،
   عرض الكل) بقت باللون الأساسي الجديد (الأخضر الزمردي) بدل الأزرق.
7. تم التحقق من توازن الوسوم بعد كل التعديلات (793 فتح / 793 قفل
   لعلامات `<``>`، و150/150 للأقواس المعقوفة).
8. ألوان زر الإغلاق الأحمر (hover/pressed) اتسابت زي ما هي لأنها
   بالفعل مطابقة تمامًا لعائلة DangerColor الجديدة، فمفيش داعي لتغييرها.

النتيجة: الإطار الخارجي بالكامل (القائمة الجانبية، الهيدر، الفوتر،
نافذة التنبيهات) بقى متماشي 100% مع هوية "الخزينة الحديثة"، وأول
استخدام حقيقي للون الذهبي جاهز في مكان منطقي (تمييز العنصر النشط).

---------------------------------------------------------------
ملاحظة مهمة عن حجم المهمة المتبقية:
---------------------------------------------------------------
فحص شامل لكل ملفات XAML في المشروع (46 ملف) لقى إن فيه ألوان صريحة
(hardcoded) بتتجاوز الموارد المركزية في **44 شاشة تقريبًا**، بعضها
فيه أكتر من 90 لون مكتوب صراحة في ملف واحد (مثلًا ProductsView.xaml
فيه 95، PurchaseInvoiceView.xaml فيه 74، StoresView.xaml فيه 61).
يعني مراجعة كل شاشة بنفس الدقة اللي اتعملت في App.xaml وMainWindow
(تصنيف كل قيمة حسب سياقها الفعلي، مش استبدال أعمى) هي مهمة كبيرة
هتحتاج تتقسم على شكل دفعات (batches) من الشاشات عبر خطوات متعددة،
مش خطوة واحدة، عشان تفضل دقيقة وآمنة ومتوافقة مع أسلوب العمل
المتفق عليه (خطوات صغيرة قابلة للمراجعة).

---------------------------------------------------------------
متبقٍ من الخطة (مقترح، لسه محتاج موافقة):
---------------------------------------------------------------
- Part 3 (مقترحة): البدء في مراجعة الشاشات الأكتر استخدامًا أولًا
  كدفعة أولى (مثلًا: DashboardView, SalesInvoiceView,
  PurchaseInvoiceView, CustomersView, ProductsView — 5 شاشات)،
  وتوحيد ألوانها الصريحة مع الهوية الجديدة، ثم الاستمرار بباقي
  الشاشات على دفعات مماثلة في الخطوات التالية.
=====================================================================
